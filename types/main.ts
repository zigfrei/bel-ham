export type NewsCard = {
  id: string;
  title: string;
  subtitle: string;
  tag: string;
  image: string;
  gradient: string;
  date: string;
  link: string;
};

export type FaqItem = {
  question: string;
  answer: string;
};